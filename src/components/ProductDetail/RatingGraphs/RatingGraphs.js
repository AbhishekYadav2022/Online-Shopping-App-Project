import React, { Component } from "react";
import styled from "styled-components";

const Container = styled.div`
  border: 2px solid green;
  margin: 15px;
  min-height: 150px;
`;

export default class RatingGraphs extends Component {
  render() {
    return (
      <Container>
        Nothing to show
      </Container>
    );
  }
}
